#def user_context(request):
    # Certifique-se de que o usuário esteja autenticado antes de acessar seus dados
    #if request.user.is_authenticated:
     #   return {
     #       'user_email': request.user.email,
     #       'user_id': request.user.id,
            # Adicione outros dados do usuário que deseja disponibilizar
     #   }
    #return {}